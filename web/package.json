{
  "name": "aether-universal-platform",
  "version": "0.1.0",
  "private": true,
  "description": "Static landing page and future in-browser VM for real Aether OS artifacts",
  "scripts": {
    "build:artifacts": "powershell -ExecutionPolicy Bypass -File ../scripts/build-web-artifacts.ps1",
    "serve": "npx --yes serve public -p 8080",
    "start": "npm run serve"
  },
  "keywords": ["aether-os", "uefi", "qemu.wasm"],
  "license": "MIT"
}
