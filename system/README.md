# System Configuration

> **Status:** Not started — planned when user-space init exists (M6).

This directory will hold default system configuration, service manifests, and
policy templates for capability delegation in early user-space bring-up.

Nothing in this directory is loaded by the kernel in M0 or M1.
